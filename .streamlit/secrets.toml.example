# Streamlit Cloud secrets
# No secrets required for this app — reads static sap_top25.json from repo
#
# If you add authentication or external API calls later, add secrets here:
# GITHUB_TOKEN = "your_token"